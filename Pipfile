[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = "*"
uvicorn = "*"
pydantic = "*"

[dev-packages]

[requires]
python_version = "3.12"

[scripts]
api = "uvicorn erp.api:app --reload --env-file .env"
api-debug = "uvicorn erp.api:app --reload --log-level debug"
